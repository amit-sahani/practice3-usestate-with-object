import logo from './logo.svg';
import './App.css';

function App() {
  return (
    <div className="App">
      <p>This is the example with setting object state without spread operator</p>

      <p>This is the example with setting object state without spread operator</p>
    </div>
  );
}

export default App;
